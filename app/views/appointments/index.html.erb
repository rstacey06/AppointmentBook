<h1>Appointments</h1>
<%= link_to "Make a new appointment", new_appointment_path %>
<%= add_weekly_calendar(@appointments) %>

<h3>Upcoming Appointments</h3>
<% if @upcoming_appointments.any? %>
  <%= render @upcoming_appointments %>
<% else %>
  <p>No appointments yet.</p>
<% end %>
